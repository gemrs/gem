local player = require("gem.game.player")
local interfaces = require("id.interface")

-- Set some revision specific interface id's that gem needs
player.set_inventory_interface_id(interfaces.inventory)
